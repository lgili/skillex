interface:
  display_name: "Token Saver"
  short_description: "Save tokens with terse technical replies, compact memory files, short commits, and one-line reviews"
  default_prompt: "Apply Token Saver in full mode: answer first, drop filler, keep technical substance exact, and use compact structures."

policy:
  allow_implicit_invocation: true
